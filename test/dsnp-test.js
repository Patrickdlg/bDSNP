/* eslint-env mocha */
/* eslint prefer-arrow-callback: "off" */

'use strict';
const assert = require('assert');

describe('DSNP', function () {
  it('dummy', async () =>{
    assert.equal(1,1);
  });
});
